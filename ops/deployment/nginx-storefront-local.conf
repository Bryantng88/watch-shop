# Local-only acceptance proxy. Production TLS and host values live in
# nginx-storefront-public.conf.example.
server {
    listen 8080;
    server_name _;

    client_max_body_size 64k;
    proxy_connect_timeout 5s;
    proxy_read_timeout 20s;
    proxy_send_timeout 20s;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    location = / { proxy_pass http://host.docker.internal:3100; }
    location = /request { proxy_pass http://host.docker.internal:3100; }
    location = /offline { proxy_pass http://host.docker.internal:3100; }
    location = /manifest.webmanifest { proxy_pass http://host.docker.internal:3100; }
    location = /sw.js { proxy_pass http://host.docker.internal:3100; }
    location = /storefront-icon.svg { proxy_pass http://host.docker.internal:3100; }
    location = /vintic-v.svg { proxy_pass http://host.docker.internal:3100; }
    location = /storefront-maskable.svg { proxy_pass http://host.docker.internal:3100; }

    location ^~ /products { proxy_pass http://host.docker.internal:3100; }
    location ^~ /api/public/ { proxy_pass http://host.docker.internal:3100; }
    location = /api/integrations/zalo/events { proxy_pass http://host.docker.internal:3100; }
    location ^~ /_next/static/ { proxy_pass http://host.docker.internal:3100; }
    location = /_next/image { proxy_pass http://host.docker.internal:3100; }

    location / { return 404; }
}
