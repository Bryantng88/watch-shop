"use client";

export default function PricePermissionNotice() {
    return (
        <div className="rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs leading-5 text-slate-600">
            Giá mua, landed cost, payment OUT, margin và nhập giá bán chỉ hiển thị cho admin. Các phần còn lại của Workbench vẫn mở cho user thường.
        </div>
    );
}
